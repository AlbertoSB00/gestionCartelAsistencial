<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <item
        android:title="@string/menu">

        <menu>

            <item
                android:id="@+id/home"
                android:icon="@drawable/icon_home"
                android:title="@string/home"/>

            <item
                android:id="@+id/doctor"
                android:icon="@drawable/icon_doctor"
                android:title="@string/doctors"/>

            <item
                android:id="@+id/reserve"
                android:icon="@drawable/icon_reserve"
                android:title="@string/reserve"/>

            <item
                android:id="@+id/settings"
                android:icon="@drawable/icon_settings"
                android:title="@string/settings"/>

        </menu>

    </item>

    <item
        android:title="@string/access">

        <menu>

            <item
                android:id="@+id/admin"
                android:title="@string/admin_mode"
                android:icon="@drawable/icon_reserve"/>

            <item
                android:id="@+id/log_out"
                android:title="@string/log_out"
                android:icon="@drawable/icon_logout"/>

        </menu>

    </item>

</menu>