<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GestionCartelAsistencial" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GestionCartelAsistencial" parent="Base.Theme.GestionCartelAsistencial" />

    <style name="Theme.GestionCartelAsistencial.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.GestionCartelAsistencial.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.GestionCartelAsistencial.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.GestionCartelAsistencial.Fullscreen" parent="Theme.GestionCartelAsistencial">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.GestionCartelAsistencial.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.GestionCartelAsistencial.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

    <style name="Theme.noactionbar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>